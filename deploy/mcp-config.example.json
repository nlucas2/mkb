{
  "//": "Example MCP client config for mkb. Point your MCP-capable agent at mkb-mcp; it speaks MCP over stdio and auto-starts the daemon for the given vault.",
  "mcpServers": {
    "mkb": {
      "command": "mkb-mcp",
      "args": ["--vault", "/path/to/your/mkb-vault"]
    }
  }
}
